%td{colspan: 6}
  = form_for object, url: object.new_record? ? items_path : item_path(object),
                     remote: true, html: { class: 'form-horizontal' } do |f|
    = f.text_field :name
    = f.submit "Save", class: "btn btn-primary"
    %button.btn.btn-link.cancel Cancel