setTimeout(function(){
  var selector = "<%= ".#{@type}s" %>";
  $(selector).html("<%= escape_javascript(render partial: 'menus/form', locals: { meals: Meal.up_to_date.send("#{@type}s"), type: @type }) %>");
}, 200);
