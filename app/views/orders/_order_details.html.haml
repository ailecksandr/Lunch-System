.panel-body
  .summ
    - Meal.meal_types.keys.each do |type|
      %div{class: type}
        .col-md-6
          %h4= "#{humanize_type!(type)}:"
        .col-md-6.text-right
          %h3= order.send(type).price
          %small UAH
    .col-md-12
      %hr
    .total
      .col-md-6
        %h4 Total:
      .col-md-6.text-right
        %h3= order.total
        %small UAH
    - if order.open?
      .col-md-12
        = link_to 'Close order', order_path(order, date: date), class: 'btn btn-danger right',
            method: :delete, remote: true, data: { confirm: 'Close that order?'}